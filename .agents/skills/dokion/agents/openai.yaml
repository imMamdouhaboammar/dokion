interface:
  display_name: "Dokion"
  short_description: "Repo-specific patterns and workflows for dokion"
  default_prompt: "Use the dokion repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true